# 砖成本材料预算 · 成本估算系统 —— 使用说明

本地 Web 版成本估算系统,依据《成本估算系统_需求文档.md》实现。
**数据存储**:本机 SQLite 数据库 `data/brick.db`(首次启动自动从旧 `data/data.json` 迁移,原文件保留;无需联网)。

---

## 一、启动

**方式 A(推荐)**:双击本目录下的 **`启动.bat`**,会弹出"砖成本估算服务"窗口并自动打开浏览器,访问 `http://127.0.0.1:8237`。**请保持该服务窗口开启**,关闭它即停止服务。

**方式 B(命令行)**:
```
cd D:\deepseek-harness\陆创\成本估算系统
node server.js
```
然后用浏览器打开 `http://127.0.0.1:8237`。

> 端口默认 8237;若被占用,可用 `PORT=9000 node server.js` 换端口(启动.bat 不变)。
> 首次启动会自动创建 SQLite 数据库 `data/brick.db`,内置原表 11 种材料与默认账号 **admin / admin123**。

---

## 〇、登录与账号

- 打开系统须先登录;未登录只能看到登录/注册页。
- **默认账号**:admin / admin123(首次登录后请在「用户管理」中尽快改密;改密按钮在你自己的行上)。
- **注册**:登录页点"注册一个",填用户名/密码(≥6 位)/昵称;**除内置 admin 外的第一个注册账号自动成为管理员**,之后的注册为普通用户。
- 顶部右侧显示"昵称(角色)· 退出";**管理员**额外看到「用户管理」标签页,可新增用户、改角色(普通/管理员)、重置密码、删除(不能删自己,且系统至少保留一名管理员);普通用户仅能使用业务功能,看不到该页。
- 登录状态存本机浏览器(localStorage token),服务重启不丢;过期(7 天)后需重新登录。

---

## 二、使用流程

### 1. 材料管理
- 预置 11 种材料:底料(黑水泥、3-6、5-10、机制砂、石粉)、面料(白水泥、精白砂、金刚黑、普砂、增强剂、染料)。
- 可新增材料、修改所属区(底料/面料)、删除(被产品配方或估算单引用的材料不能删)。

### 2. 产品配方
- 「新建产品」→ 填名称规格、产品编号 → 从材料列表添加配方明细,填写每种材料**每锅用量(公斤)** → 保存。
- 配方属于产品档案;同一产品多次估算共用配方(每次估算可临时调整数量)。

### 3. 估算单(核心 · Excel 化表格)
- 打开系统**默认进入最近一个估算单的 Excel 化表格**(主界面),版式与《砖成本材料预算表_黑白打印版.xlsx》一致:标题/表头 → **砖规格行** → 底料配比 · 面料配比 → 材料清单(公斤) → 成本核算。
- **砖规格行(第 3 行)**:砖长/宽/高(mm,必填黄底),右侧自动显示 **砖面积 = 长×宽÷1e6(m²)**、**砖体积 = 长×宽×高÷1e9(m³)**。
- **使用方法与 Excel 相同**:
  1. 点击任一可编辑单元格(白底=输入格、蓝底=公式格;**黄底=必填待填写**),顶部公式栏显示该格地址与内容;
  2. 在公式栏修改数值或输入公式(以 `=` 开头,如 `=C6*D6`、`=SUM(E6:E11)`),**回车生效**,所有依赖格自动重算;
  3. 清空公式格内容可恢复默认公式;公式写错显示 `#DIV/0!`、`#VALUE!`、`#NAME?`、`#CYCLE!`(循环引用)等错误。
- 支持函数:`SUM / AVERAGE(AVG) / MIN / MAX / COUNT`(含区域)、`IF`、`ROUND`、`ABS`、`INT`;四则运算、括号、比较(=、<>、<、>、<=、>=)。
- **单位与金额口径**:材料清单一律**公斤**,金额 = 单价 × 数量(公斤)**直乘不换算**;本期用料数量 = 配比每锅 × 锅数(公斤,不 ÷1000);成本总价② = 材料吨价 × 总用料(公斤)。
- **自动派生公式**:每平方重量 = (1/砖面积)×每块重量;每立方重量 = (1/砖体积)×每块重量;每托重量 = 每托平方×每平方重量;计划数 = 模数×每模块数×单砖面积(m²);成品率 = 实际数÷计划数;占比 = (每锅×锅数)÷全材料总和;模数 = 止模−始模+1;成本总价①/②。均可点击查看并覆盖修改。
- **必填项(黄色高亮)**:名称规格、产品编号、开始/结束日期、编制人、砖长/宽/高、每种材料的单价与每锅数量、底料/面料锅数、每块重量、每模块数、模数、每托块数、每托平方。填入后高亮自动取消;**未填完只能保存为草稿,全部填完才能保存为可用表格**。
- **保存与退出**:工具栏「存为草稿 / 保存为可用表格 / 保存为副本」;**退出编辑时弹窗三选**。估算单列表带「草稿 / 可用」状态标记。
- **相邻批次校验**:同产品编号的相邻批次若"前一单结束日期晚于本单开始日期"或"止模不小于后单始模(模数未递增)",保存时会弹确认提醒(仍可确认保存)。
- 增减材料:下方「添加材料行」选择材料;「当前材料」行上的 ✕ 可移除(行结构变化会清空公式覆盖)。

### 4. 图表预览
- **材料成本构成环形图**:单批次各材料本期用料金额占比(右上角可切换批次,点击扇区可跳到该批次);
- **批次成本总价对比柱状图**:各批次成本总价①/② 并排对比(点击柱子打开对应批次);
- **批次成本趋势折线图**:按开始日期排序的成本总价①/② 走势(≥2 个批次时显示);
- **材料用量/金额汇总条形图**:跨批次按材料汇总,可在"按金额 / 按用量(公斤)"间切换。
- 图表基于当前全部估算单实时生成,新增/修改估算单后回到本页即刷新。

### 5. 历史对比
- 勾选 ≥2 个估算单,并排对比:锅数、本期用料合计、成本总价①/②、模数、成品率及每种材料的用料数量与金额。

---

## 三、计算规则(与需求文档一致)

| 项目 | 规则 |
|---|---|
| 单位 | 全表数量一律**公斤**,金额 = 单价 × 数量(**直乘,不做 ÷1000**) |
| 配比金额(每锅) | 单价 × 数量(公斤) |
| 配比占比 | (该材料每锅 × 所在区锅数) ÷ (底料每锅×底料锅数 + 面料每锅×面料锅数)(跨区按锅数加权) |
| 库存材料 | 上存材料 + 本期进料 |
| 材料清单金额 | 单价 × 数量(公斤) |
| 本期用料数量 | 配比每锅数量 × 对应区锅数(公斤,自动,**不换算**) |
| 本期用料单价 | = 单价(自动) |
| 本期用料金额 | 本期用料单价 × 本期用料数量(公斤) |
| 清单占比 | 本期用料数量 ÷ 本期用料数量合计(**按使用数量**) |
| 砖面积 / 砖体积 | 长×宽÷1e6(m²)/ 长×宽×高÷1e9(m³)(自动) |
| 每平方重量 / 每立方重量 | (1/砖面积)×每块重量 / (1/砖体积)×每块重量(自动) |
| 每托重量 | 每托平方 × 每平方重量(自动) |
| 计划数 | 模数 × 每模块数 × 单砖面积(m²)(自动) |
| 成品率 | 实际数 ÷ 计划数(自动) |
| 材料吨价 | 本期用料金额合计 ÷ 本期用料数量合计 ÷ 1000(自动) |
| 每平方价 | 本期用料金额合计 ÷ 实际数;实际数未填时 ÷ 计划数(自动) |
| 模数 | 止模 − 始模 + 1(自动) |
| 成本总价① | 本期用料金额合计(自动) |
| 成本总价② | 材料吨价 × 本期用料总量(公斤)(自动,直乘) |
| 防错 | 任何分母为 0/空时结果为空或按 0 处理 |

> 注:材料吨价/每平方价已改为按上表公式自动计算(蓝底公式格,可点开查看或覆盖);②口径按"公斤直乘"直接相乘。

---

## 四、数据存储与备份(SQLite)

- **数据库**:数据保存在本机 SQLite `data/brick.db`(表:材料/产品配方/估算单/估算单行/用户/会话等),事务写入、WAL 模式,崩溃不易损坏;
- **在线快照(B3)**:页面顶部「快照备份」一键执行 `VACUUM INTO` 一致性快照,存于 `data/backups/`,自动保留最近 30 份;
- **导出/导入(B2)**:「导出备份文件」下载单文件 `brick-data-日期.zip`(含业务数据+元信息,可拷走/传网盘);「导入恢复」选择该 zip(或 .json)即可还原——**导入前系统会自动先做一次快照**,可随时回退;
- **人工云同步建议**:定期把 `data/backups/` 或导出的 zip 复制到公司网盘/文件服务器,并每月做一次"用最近备份恢复"演练;
- **旧版兼容**:升级前若在 `data/data.json` 存过数据,首次启动新服务会自动迁移进 SQLite,原文件保留不动;
- 想恢复出厂(清空产品/估算单、重置材料):页面内手动删除后,或调用 `POST /api/reset`,再或停止服务删除 `data/brick.db` 后启动(会重新内置 11 种材料)。

> 备份按钮在页面顶部深色栏「数据备份:快照备份 / 导出备份文件 / 导入恢复」。

---

## 五、常见问题

| 问题 | 处理 |
|---|---|
| 打开页面停在登录/注册页 | 正常:先登录。默认账号 admin/admin123;忘记密码由管理员在「用户管理」重置 |
| 提示"用户名或密码错误" | 核对大小写与长度;若刚注册,先用"注册"入口创建账号(首个注册为管理员) |
| 提示"登录已过期,请重新登录" | 会话有效期 7 天;重新登录即可,登录状态存本机浏览器 |
| 打开页面提示"无法连接本地服务" | 未启动服务,先双击 启动.bat |
| 端口被占用 | 关闭占用程序,或 `PORT=9000 node server.js` 后用 `http://127.0.0.1:9000` |
| 修改了文件页面没变化 | 浏览器按 Ctrl+F5 强制刷新 |
| 数据找不到了/想回退 | 用页面「导入恢复」选最近一次导出 zip,或从 `data/backups/` 复制最近快照恢复 |

---

## 六、开发与验证(可选)

系统零第三方依赖(仅 Node 内置模块),测试亦为零依赖(Node ≥ 22,内置 fetch/WebSocket):

```
node test/calc.test.cjs       # 计算逻辑单元测试(全部公式)
node test/formula.test.cjs    # 公式引擎单元测试(引用/函数/错误/循环)
node test/sheet.test.cjs      # 表格默认公式与 CostCalc 口径一致性测试
node test/seed-demo.cjs       # 重置演示数据(11 种材料 + 1 产品 + 2 估算单)
node test/auth-api-check.cjs  # 服务端鉴权冒烟(注册首个 admin/401/403/用户 CRUD)
node test/db-backup-check.cjs # 备份导出 zip → 修改 → 导入恢复 验证
node test/cdp-test.cjs        # 无头浏览器主流程测试(联动/标签页/对比)
node test/cdp-grid-test.cjs   # 无头浏览器表格编辑器测试(选择/公式覆盖/还原/循环/保存)
node test/cdp-rules-test.cjs  # 无头浏览器规则测试(必填高亮/草稿限制/退出弹窗/相邻校验)
node test/cdp-crud-test.cjs   # 无头浏览器 CRUD 流程测试(材料/产品/估算单)
node test/cdp-charts-test.cjs # 无头浏览器图表页测试
node test/cdp-auth-test.cjs   # 无头浏览器登录/注册/登出/用户管理/越权测试
node test/cdp-fix-check.cjs   # 滚动保持/零异常/图标验证
```

> 注:初始交付自带一条演示产品「标砖 240×115×53」与两个示例估算单,便于上手;不需要可删除,或运行 `node test/seed-demo.cjs` 复位。
