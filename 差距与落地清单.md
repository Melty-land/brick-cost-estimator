# 距离实际运用:差距与落地清单

> 项目:砖成本材料预算 · 成本估算系统(本地 Web)
> 版本基线:当前已实现功能 + 全部自动化测试通过
> 目的:客观列出从"能跑、能算、有测试"到"可日常实际运用"之间尚未落地的地方,以及建议的实施顺序。

---

## 一、目前已经可用的部分(简述)

- 本地 Web 应用,一键 `启动.bat` 启动;材料/产品配方/估算单管理,多批次历史与对比、图表预览;
- Excel 化估算单编辑:单元格直接编辑 + fx 公式栏 + 自定义公式(=引用/SUM/IF/ROUND 等)、自动重算、循环/除零检测、公式覆盖随单保存;
- 草稿/可用表格两级保存、必填黄底高亮引导、退出三选弹窗、相邻批次(同产品编号)日期/模数衔接校验;
- 材料清单公斤口径、砖长宽高/面积/体积及每平方·立方重量、每托重量、计划数、成品率、材料吨价、每平方价等自动派生公式;
- 数据本地持久化(data/data.json)、演示数据与 9 套自动化测试(单元 + 浏览器 E2E)。

---

## 二、距实际运用尚未落地的差距

### 1) 业务口径与公式仍待"真实数据"校验(最重要)

| 问题 | 现状 | 建议 |
|---|---|---|
| 单价/金额单位口径 | 全表"公斤直乘不换算",单价数字按 元/公斤 语义使用;但表格与文档仍沿用了"元/吨/材料吨价"等名称 | 用真实采购价+实际生产数据核算一轮;统一并改正名称与单位注释,或按实际口径调整公式 |
| 成本总价② | 材料吨价改为自动推导后,② ≈ ① ÷ 1000,已失去"独立验算"意义 | 与业务确认②的意义(吨价基准? 每千公斤成本?),必要时改回独立手动值或换基准 |
| 计划数 | 按选定字面公式"模数×每模块数×单砖面积(m²)",示例仅 ~1.66(数量级小,疑为口径问题) | 与生产计划实际口径核对(每模块数/模数含义、是否按每平方块数等),可能需改公式 |
| 每平方价 / 成品率 | 按公式自动(金额÷实际或计划;实际÷计划);实际数为空时每平方价会很大 | 确认"实际数/计划数"录入习惯与单位;加入合理值范围提示 |
| 砖面积/体积单位 | 长宽高按 mm、面积 m²、体积 m³(÷1e6/÷1e9) | 与实物测量单位核对 |
| 占比/配比口径 | 跨底/面料按(每锅×锅数)加权;材料清单公斤化后与"吨"历史习惯不同 | 用真实批次数据比对旧 Excel 口径 |

### 2) 功能与体验未落地

- **导出/打印**:尚无"按原黑白打印版版式导出 Excel/PDF/打印"能力;实际场景(上报、归档、签字)需要;
- **Excel 导入**:无法导入历史《砖成本材料预算表_黑白打印版.xlsx》数据迁移;
- **材料主数据价格**:材料无默认单价,新建估算单单价靠手填;
- **产品配方版本管理**:产品配方修改即覆盖,无版本/变更历史;估算单导入配方是"当前快照"复制,无"与产品联动/过期提示";
- **数据校验范围有限**:负数、单价/用量合理区间、库存不足(本期用料>库存)、日期顺序(开始≤结束)等仅部分有校验;
- **移动/触摸端**:表格编辑针对键鼠,平板/手机不便;
- **错误体验**:公式写错仅显示 #NAME?/#VALUE! 等,无"错误定位/函数向导/自动补全"帮助。

### 3) 数据与运维差距

- **备份自动化**:数据只有手动复制 data/data.json;无定时备份、版本化备份、一键导出/导入(Web 侧无导出按钮);
- **多用户/权限**:单机单人,无账号、无只读/编辑权限区分;
- **部署形态**:依赖本机 Node.js 环境;无常驻服务/开机自启/托盘图标;换机器需重装依赖与搬数据;
- **端口与冲突**:固定 8237,占用时需手动换端口;
- **日志/审计**:无操作日志(谁在何时改了哪张单)、无错误上报;
- **数据安全**:无数据库/加密,json 明文;多人共用目录时无并发控制(可能互相覆盖)。

### 4) 代码与质量层面(距"可维护产品"的差距)

- **浏览器兼容**:自动化仅在无头 Edge 验证;Chrome/旧版浏览器未全面测;
- **性能**:表格为整页重绘 + 全网格重算;材料行很多(几十~上百行)、批次很多时无性能压测与虚拟滚动;
- **健壮性**:断电/强杀时 json 写一半损坏无恢复;无数据文件校验;
- **测试**:逻辑与 E2E 覆盖较好,但缺少:真实业务样例库、并发/大数据量、多浏览器矩阵、备份恢复演练。

### 5) 尚未形成闭环的业务场景

- 与**采购(单价/进价单)**、**库存(上存/进料自动取数)**、**生产报工(实际数/成品率)**对接自动取数——当前全部手填;
- **审批/流程**:草稿→提交→审核→归档;
- **统计报表**:按月/按产品汇总成本趋势、采购建议、异常预警(成品率低于阈值等)目前仅基础图表。

---

## 三、建议落地顺序(按价值/风险排序)

1. **P0 数据口径校准(先用真数据走通一周)**
   用真实产品或物料录入 3~5 张估算单,核对:单价口径、①/②、计划数与成品率、面积体积、占比——把上表"业务口径"问题全部消掉;
2. **P1 数据安全与运维**
   自动备份(启动时/定时复制 data.json + 保留 N 份)、Web 端一键导出/导入备份、开机自启脚本/启动器、写入前文件校验与原子写(写临时文件再替换);
3. **P1 导出/打印 + Excel 迁移**
   输出与原黑白打印版一致的 Excel/PDF;支持导入旧表数据生成草稿;
4. **P1 校验与友好提示**
   公式帮助/错误定位、合理值校验、日期顺序与库存不足提醒;
5. **P2 流程与管理**
   材料默认单价、产品配方版本、草稿→提交→归档状态流、多用户(简单账号+只读);
6. **P2 接入业务系统/报工**
   库存与生产实际数自动同步(Excel 或接口),月报/预警报表。

---

## 四、结论

系统**功能骨架与质量底座已经成型**(结构化录入、公式化计算、两级保存与校验、图表与历史、自动化测试),但目前更接近"能正确计算的自用工具",距"生产环境日常使用"还差三件事:**用真实数据把计算口径校准**、**把数据安全/备份与导出打印补上**、**把物料/库存/报工数据源打通**。建议先做 P0 的真实数据试运行(通常 1~2 周),再按上表 P1→P2 推进。

(本清单随迭代更新;每完成一项请同步修改本文件并提交 git。)
