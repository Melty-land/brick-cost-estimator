# 数据库与数据备份方案(设计稿 · 供选择)

> **落地状态:已实施(2026-09)** —— 用户选定「方案二:SQLite 内置数据库 + B3 在线快照 + B2 一键导出/导入」。
> 存储改为 `data/brick.db`(node:sqlite),保留 /api/data 兼容层并自动迁移旧 data.json;
> 页面顶部新增「快照备份 / 导出备份文件 / 导入恢复」(导入前自动快照)。本文件保留原始设计稿与备选方案说明。

> 覆盖范围:材料管理、产品配方、估算单、用户账号、会话等全部数据。
> 背景:现状为"单 JSON 文件 + 本地 Node API"(无数据库、无事务、无鉴权);
> 用户已用真实数据试运行一周,计算层基本稳定;现进入"持久性与安全加固"设计阶段。

---

## 〇、要覆盖的数据范围(所有方案都需支持)

| 领域 | 数据 |
|---|---|
| 材料管理 | 材料 id、名称、所属区(底料/面料)、排序、创建时间 |
| 产品配方 | 产品 id、名称规格、产品编号(产品档案,如 BZ-240);配方明细(材料×每锅用量,有序) |
| 估算单 | 估算单(id、关联产品、名称、预算表编号 code(独立手填,不再随产品自动带入)、状态 draft/ready、编制人、起止日期、底/面料锅数、成本核算参数、公式覆盖 JSON、时间戳)+ 估算单材料行(每行:材料快照、每锅用量、单价、上存/进料/数量) |
| 用户账号 | 用户名、昵称、角色(admin/user)、状态(active/disabled,停用即踢下线)、密码(加盐哈希)、创建时间 |
| 会话 | token、用户、过期时间(登录用) |
| 元信息 | 结构版本号(便于迁移)、备份记录 |

---

## 方案一:JSON 文件存储强化(现状演进,零依赖)

**结构**:`data/`
- `data.json` 业务数据(材料/产品/估算单)
- `users.json` 用户(加盐哈希密码,服务端独占)
- `tokens.json` 会话
- `backups/` 自动备份目录

**要点**:
- 写入改为**原子写**(先写临时文件再 rename 覆盖),崩溃不产生半截文件;
- 每次写前做 JSON 合法性校验,写入 `schemaVersion` 字段;
- 单机单人场景足够,改动最小(前端/API 结构基本不变)。

**优点**:零依赖、文件可直接查看/复制/迁移、当前功能不改动、风险最低。
**缺点**:无 SQL、无事务(写整包)、数据量大时全量读写较慢;仅适合单机/少量用户。

---

## 方案二:SQLite 内置数据库(推荐 · 零第三方依赖)

利用 Node 24 内置 `node:sqlite`(已在目标机验证可用),真数据库、单文件 `data/brick.db`。

**表结构(DDL 概要)**:

```sql
CREATE TABLE materials (
  id TEXT PRIMARY KEY, name TEXT NOT NULL, zone TEXT NOT NULL CHECK(zone IN ('底料','面料')),
  sort INTEGER DEFAULT 0, created_at TEXT
);
CREATE TABLE products (
  id TEXT PRIMARY KEY, name TEXT NOT NULL, code TEXT, created_at TEXT, updated_at TEXT
);
CREATE TABLE product_recipe (
  product_id TEXT NOT NULL REFERENCES products(id) ON DELETE CASCADE,
  seq INTEGER NOT NULL, material_id TEXT NOT NULL REFERENCES materials(id),
  qty_per_pot REAL NOT NULL DEFAULT 0, PRIMARY KEY (product_id, seq)
);
CREATE TABLE estimates (
  id TEXT PRIMARY KEY, product_id TEXT REFERENCES products(id),
  name TEXT, code TEXT, status TEXT CHECK(status IN ('draft','ready')),
  author TEXT, start_date TEXT, end_date TEXT,
  pot_bottom REAL, pot_top REAL,
  calc TEXT,            -- 成本核算参数字段(JSON)
  formulas TEXT,        -- 单元格公式覆盖(JSON)
  created_at TEXT, updated_at TEXT
);
CREATE TABLE estimate_rows (
  id TEXT PRIMARY KEY, estimate_id TEXT NOT NULL REFERENCES estimates(id) ON DELETE CASCADE,
  seq INTEGER NOT NULL, material_id TEXT, name TEXT, zone TEXT,
  qty_per_pot REAL, price REAL, stock_on_hand REAL, stock_in REAL, qty REAL
);
CREATE TABLE users (
  username TEXT PRIMARY KEY, nickname TEXT, role TEXT CHECK(role IN ('admin','user')),
  salt TEXT NOT NULL, hash TEXT NOT NULL, created_at TEXT
);
CREATE TABLE sessions (token TEXT PRIMARY KEY, username TEXT, exp INTEGER);
CREATE TABLE meta (key TEXT PRIMARY KEY, value TEXT);  -- schemaVersion 等
```

**要点**:
- 估算单及其行在**一个事务**内写入/更新,崩溃安全(WAL 预写日志);
- 支持 SQL 查询(按产品/日期/状态统计报表),天然防"半截写";
- 用户/会话入库,为登录页与用户管理打好底;
- 启动时检测旧 `data.json` → **一次性迁移导入 SQLite**(迁移脚本),原文件另存备份;
- `node:sqlite` 目前属内置但标记实验性:本项目 Node 24.15 无 flag 可正常运行,已实测。

**优点**:真正的数据库(事务/SQL/索引/并发读安全)、可靠性最高、便于后续报表与多人共享;
**缺点**:需按表结构改写数据层(Store/API),迁移有一定工作量;实验性 API 需跟随 Node 版本关注。

> 备选(若未来放弃内置 API):`better-sqlite3` 为成熟同步库,但需要 npm 安装(当前环境 npm 源不通,需联网后安装)。

---

## 方案三:JSON 主存 + SQLite 只读归档(渐进式)

- 日常编辑仍走 JSON(改动最小、功能零回归);
- 启动时或每日把全部数据导入一份 SQLite "存档库",用于**只读查询/报表/审计/恢复**;
- 之后若体验好,再平滑切换到方案二。

**优点**:先不冒险、双保险、渐进。
**缺点**:两套数据维护成本;JSON 自身的写安全问题仍存在。

---

## 备份方案(可独立选择/组合)

| 编号 | 方案 | 说明 |
|---|---|---|
| B1 | **自动备份 + 轮转** | 启动时/每小时/每天各留最近 N 份(如启动保留 20、每日保留 30)到 `data/backups/<时间戳>/`;恢复即选最近一份替换。支持任一种数据库方案。 |
| B2 | **Web 一键导出/导入** | 系统内"数据备份"页:一键导出单文件 `brick-data-YYYYMMDD-HHmm.zip`(业务数据+用户+版本),可拷走/上传网盘;一键导入恢复(导入前自动再备份当前)。 |
| B3 | **数据库在线快照**(仅选 SQLite 时) | 用 `VACUUM INTO` 生成一致性 .db 快照 + 轮转,不中断服务、不会拷到写一半的数据。 |
| B4 | **人工云同步指引** | 使用说明中引导把 `data/backups/` 或导出 zip 同步到公司文件服务器/网盘;定期(如每周)人工验证一次"用最近备份能恢复"。 |

**建议组合(默认)**:
- 若选 **方案一(JSON)**:B1 + B2;
- 若选 **方案二(SQLite)**:B3 + B2(可再加 B1 对 db 文件冷备);
- 无论哪种,均配合 **B4 人工云同步** 与"每月一次恢复演练"。

---

## 待你选择(选择后我再出详细实施计划)

- **Q1 数据库方案**:方案一(JSON 强化)/ 方案二(SQLite 内置,推荐)/ 方案三(JSON+SQLite 归档)
- **Q2 备份组合**:B1 / B2 / B3(仅 SQLite 可选)/ 组合(推荐 B1+B2 或 B2+B3)

> 备注:此前的"登录页+用户页"需求将基于最终选定方案的 users/sessions 存储一并落地;
> 无论选哪个,现有 `data.json` 数据都可平滑迁移,不会丢现有估算单。
