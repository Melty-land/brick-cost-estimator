# 登录 / 注册功能方案(已实施)

> 状态:**已按「方案 A 全站需登录 + 开放注册·首个为管理员」实施完成**,含服务端接口、前端登录/注册/退出/用户管理、全部测试改造与新增浏览器 auth 套件。
> 前置:数据已迁移至 SQLite(`data/brick.db`),users/sessions 表在方案定稿时已建好并投入使用。

---

## 一、目标

1. 打开系统须先**登录**;提供**注册**入口;
2. 账号数据存 SQLite(`users` 表:用户名/昵称/角色/加盐密码哈希/状态/创建时间);
3. 会话用**服务端签发 token**,存在 `sessions` 表(重启不丢登录),过期时间 7 天;
4. 权限:普通用户可正常使用全部业务功能;**admin 可进入"用户管理"页**(增删用户、改角色、重置密码);
5. 业务数据接口**全站需登录校验**,未登录请求返回 401。

---

## 二、接口(全部已实现,见 server.js)

| 方法/路径 | 说明 | 权限 |
|---|---|---|
| POST /api/auth/register | 注册 `{username,password,nickname}`;校验用户名唯一/密码长度≥6;**首个注册自动为 admin** | 开放 |
| POST /api/auth/login | 登录 `{username,password}` → `{token,user}` | 开放 |
| GET  /api/auth/me | 校验 token 返回当前用户 | 登录 |
| POST /api/auth/logout | 注销,删除该 token | 登录 |
| PUT  /api/auth/password | 修改自己密码(需旧密码) | 登录 |
| GET/POST /api/users | 用户列表 / 新建用户 | admin |
| PUT/DELETE /api/users/:username | 改昵称/角色/密码 / 删除 | admin(不可删自己、须保留≥1 admin) |
| (保护)GET/PUT /api/data | 业务数据读写 | 登录(全站需登录) |
| POST /api/reset | 清空业务数据并重建 11 材料 | admin |
| POST /api/db/backup / export | 在线快照 / 导出 zip | 登录 |
| POST /api/db/import | 导入 zip/json 恢复 | admin |

安全细节:密码 scrypt 加盐哈希存储,不存明文;token 为随机 48 位十六进制,存 sessions(含过期时间),服务端逐请求校验;**默认账号 admin/admin123,登录后建议立即改密**。

---

## 三、已确认策略(实施依据)

1. **鉴权范围:方案 A 全站需登录** —— 业务数据读写接口都要求合法 token;打开页面未登录只看到登录/注册页。
2. **注册策略:开放注册,首个注册者为管理员** —— 判定逻辑:`users` 表中除内置 `admin` 外无其他用户时,该注册自动为 admin;其后注册者为普通用户,注册后登录即可使用。

---

## 四、前端交互(已实现,见 public/js/app.js + store.js + index.html)

1. **登录/注册页(整页覆盖层)**:品牌标题 + 用户名/密码(注册另填昵称),错误提示,回车提交;登录成功进入主界面;注册成功后自动转登录页并提示(首个注册提示"已设为管理员")。
2. **顶部用户栏**:右侧显示"昵称(角色)· 退出";admin 额外显示"用户管理"入口标签页。
3. **用户管理页(admin)**:表格(用户名/昵称/角色/创建时间/操作)+ 新增(用户名/昵称/初始密码/角色)、改角色下拉、重置密码、删除;保护规则同接口(不能删自己/至少保留一个 admin)。
4. **会话处理**:前端把 token 存 localStorage(`brick_token`);启动时 `GET /api/auth/me` 校验,失效则回登录页;接口 401 时统一登出并提示"登录已过期"。
5. **普通用户访问 #users 被拒**:toast"仅管理员可访问用户管理"并重定向到 #estimates。

---

## 五、测试

- 服务端:`node --check` + `node test/auth-api-check.cjs`(注册首个 admin/401/403/用户 CRUD/数据接口保护,全通过);
- 浏览器:`node test/cdp-auth-test.cjs`(登录层出现、错误口令、注册首个=admin、admin 用户管理新增/删除用户、普通用户无用户管理权限、登出,全通过);
- 既有套件统一走 `?autologin=1`(boot 自动以 admin 登录,仅测试用),seed/备份脚本先登录取 token 再调接口——全部回归通过。

---

## 六、安全与运维建议(内网/单机前提)

- **务必修改默认密码后再正式使用**(默认 admin/admin123 仅在首启时创建);如暴露到局域网,建议用 `http://IP:8237` 访问并注意信任边界(轻量账号体系,非企业级 SSO);
- 会话清理:过期 sessions 在启动/登录时惰性删除;
- 想彻底锁死注册,可在服务启动后用环境变量或改 `server.js` 中 register 分支(不推荐改动,除非有明确需求)。
