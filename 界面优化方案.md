# 前端界面优化方案 —— 砖成本材料预算系统

> 依据 ui-ux-pro-max 设计库(design_recommend / design_review / design_search)输出。
> 硬约束:**零第三方依赖、完全离线可用**(无 CDN/外部字体/图标库/框架),纯 HTML+CSS+原生 JS。
> 目标:在**不改变功能与计算口径**的前提下,把"内网工具风"提升为**专业生产看板质感**,同时修掉评审出的 UX 缺陷。

---

## 一、设计方向(定调)

**产品定位**:制造业 B2B 内网成本核算/生产台账工具。核心使用场景 = 盯数据表、录估算单、看图表、做对比。

**推荐风格(依序)**:
1. **Data-Dense Dashboard(主基调)** —— 数据看板/表格密集信息是第一诉求:多图表、数据表、KPI 卡、紧凑网格、最大数据可见性。
2. **Trust & Authority(品牌基调)** —— 深蓝权威、可信、专业;衬托"材料成本"的业务严肃性。
3. Drill-Down Analytics(交互补充)—— 估算单"列表 → 明细表格 → 单元格"的层级钻取已存在,优化其上下文保持与返回路径。

**气质**:专业蓝 + 中性灰 + 琥珀强调(金额/警示)。避免嬉皮/紫粉渐变。
**密度**:6/10 标准偏紧;数据区(表格/图表)更密,功能区(表单/按钮)留呼吸。

---

## 二、配色系统(全部替换为语义化 CSS 变量)

现状:散落的硬编码 hex(`#1f2d3d #f5f6f8 #9aa2ae #e8ebef …`)。方案:引入设计令牌,浅色为基线。

### 浅色主题令牌(建议值)

| 令牌 | 值 | 用途 |
|---|---|---|
| `--bg` | `#F4F6FA` | 页面底(比现 #f5f6f8 略冷,更衬白卡) |
| `--surface` | `#FFFFFF` | 卡片/视图底 |
| `--surface-2` | `#F8FAFC` | 次级面(输入底/斑马纹) |
| `--primary` | `#1E40AF` | 主色(头部/主按钮/激活态/链接) |
| `--primary-hover` | `#1E3A8A` | 主色 hover |
| `--primary-soft` | `#EFF4FF` | 主色浅底(激活标签/选中行) |
| `--secondary` | `#3B82F6` | 次强调(公式蓝字/次级按钮描边) |
| `--accent` | `#B45309` | 强调/金额重点(琥珀深,保证 4.5:1) |
| `--accent-soft` | `#FEF3C7` | 必填黄底/草稿标签底(替代 #fff3c4) |
| `--danger` | `#DC2626` | 危险/删除/错误 |
| `--success` | `#16A34A` | 成功/可用标签 |
| `--text` | `#1F2937` | 主文字 |
| `--text-2` | `#4B5563` | 次级文字 |
| `--text-3` | `#6B7280` | 弱化/占位 |
| `--border` | `#E5E7EB` | 常规边框 |
| `--border-strong` | `#D1D5DB` | 表格网格线 |
| `--focus-ring` | `#2563EB` | 焦点环 |

### 头部深蓝方案
现状 `#1f2d3d`(近黑蓝)偏闷。建议:保留深蓝头部以维持"权威/稳重",改用 `#1E3A8A→#1E40AF` 渐变(160°),文字用白 + 15% 透明底按钮,与登录页渐变呼应。

### 图表色板(替换 charts.js PALETTE)
现有 12 色接近但可统一为仪表盘语义色:
`#2563EB #F59E0B #10B981 #EF4444 #8B5CF6 #06B6D4 #F97316 #84CC16 #EC4899 #64748B #0EA5E9 #A3E635`
(前 4 个语义主用,后 8 个补充;环形图占比仍按顺序取)

### 深色主题(第二阶段,可选)
全部颜色经令牌映射;文本 ≥4.5:1、次要 ≥3:1。表格/图表在深色下独立验证。**建议先做浅色打磨,深色作为可选后续**(本项目用户为办公室日间使用)。

---

## 三、字体与排版

现状:全站统一 13-14px `Microsoft YaHei`;标签 12px;行高 26px。

**约束**:不能引外部 Google Fonts(离线)。方案:用系统字体栈分层 + 字号层级规整:
- 数字/金额采用等宽友好的栈:`"Cascadia Mono", "SF Mono", Consolas, "Roboto Mono", monospace`(仅表格数值格与 KPI 金额)——数字对齐、单价一眼可比;**不引入任何外部字体文件**。
- 中文 UI 字体栈:`system-ui, "Segoe UI", "Microsoft YaHei", "PingFang SC", sans-serif`。
- 字号层级:页面标题 18/600、区块标题 16/600、正文 14、辅助 12.5、表格数据 13、KPI 数字 22-24/600。
- 行长:列表/说明文本不设满宽(主区 max 已有 1280)。

---

## 四、布局与信息架构(不改路由与功能)

### 1. 头部(现为深色条 + 标签 + 系统栏三行堆叠)
问题:三个 tab 一行、用户栏右置、sysbar 又占一行,信息密度低。
方案:
- **单行主导航条**:左侧 logo/系统名(可缩),中间 6 个标签,右侧用户徽章 + 退出 + 快照/导出(图标化)。
- **"数据备份"下沉**:sysbar 从头部移出,改放估算单/材料页内右上角,或用头部右侧"备份 ▾"菜单收纳(快照/导出/导入)。头部恢复清爽。
- 若保持 sysbar 在头:用 flex-wrap + 更细分隔,弱化 hint 文字(`color: rgba(255,255,255,.55)`、字号 12)。

### 2. 主区卡片
- `.view` 现为白卡 + 灰边 + 16px padding。统一为:**12-16px padding + 1px `--border` + 8px 圆角 + 极淡投影** `0 1px 2px rgba(16,24,40,.04)`。
- 卡片间间距 16→ 统一 20;区块标题 `sec-title` 加**左边条**已是好习惯,保留并统一高度 18px。
- 编辑器视图(`editor-wide`)已有;优化其 `.sheet-hint`/必填条为图标化提示条。

### 3. 首页默认(已改为估算单列表)
优化估算单列表为"半看板":顶部统计条(总单数、可用/草稿、成本合计、近 30 天)若可行可加;列表行保留现信息,但:
- 状态标签(可用/草稿)改为更清晰的圆角 pill(绿/琥珀底)。
- 成本总价金额列右对齐、等宽字体、琥珀 `--accent`。
- 操作按钮组(查看/复制/删除)移行尾、hover 才显示删除红。

### 4. 表格(材料/产品/估算单/用户管理 `.grid`)
- 表头:浅灰底 `--surface-2`、600、13px、sticky(页面滚动时表头固定)。
- 斑马纹(偶数行 `--surface-2` 或透明)+ **行 hover 高亮**(`--primary-soft`)。
- 数值列右对齐、等宽;文字列左对齐;删除等危险操作仅 hover 显现。
- 行高 36-40px(评审建议紧凑行高 36px 基线)。

### 5. Excel 化预算表(.sheet-grid)
- 已修复宽高与标签换行。补充:
  - 表头行(`colh`)做 sticky(编辑器内纵向滚动时 A-L 表头可见)。
  - 首列(标签列)与首行做淡底区分区块(底料/面料/清单/成本核算标题行已是 `sech`,可再统一为深一档)。
  - 必填黄格、公式蓝格、错误红格沿用**语义色令牌**。
  - 单元格 hover 轻微高亮;选中格已有蓝 outline,保留。

### 6. 图表区
- 卡片化每张图(现已有 chart-card),统一 14px 内边距。
- 图例字号 12.5、金额格式化已好;条形图末端值改为右对齐等宽。
- 空态文案(暂无数据)加图标(内联 SVG)而非纯文字。

---

## 五、图标策略(零外部依赖)

评审红线:**禁用 emoji 作结构图标**(现有 ⚠/✓/✕ 用于提示条与必填警告,属状态符号,保留;但**导航/按钮/操作不应用 emoji**——现状按钮均为文字,无 emoji 图标,合格)。

建议:内联 SVG 图标集(手写 8-12 个常用:备份、导出、导入、加号、删除、编辑、返回、搜索、用户、退出、快照)。统一 16px、stroke=1.8、与文字基线对齐。为按钮加 `data-icon` 类,图标 + 文字间距 6px。

---

## 六、UX 缺陷修复清单(design_review 命中,可落地项)

| 严重度 | 问题 | 修复 |
|---|---|---|
| 严重 | 输入缺可访问 label | 网格单元格已用 td+title;产品/材料表单的 label 已 `<label><span>` 包裹——补 `for`/`aria-label`,并确保 placeholder 不作为唯一标签 |
| 高 | 表单提交无反馈 | 保存按钮加 loading(禁用+文案「保存中…」),成功/失败 toast 已存在,补按钮态 |
| 高 | 错误只标红 | 错误消息给具体文案(已多为 toast 文案),表单内联错误加 `role=alert` |
| 高 | 长列表无虚拟化 | 材料/估算单量级小(几十),暂缓;但表格 CSS 需 `content-visibility:auto` 缓解(可选) |
| 高 | 键盘焦点可见 | 统一 `:focus-visible` 环(2px `--focus-ring`);按钮/标签补 `cursor:pointer`(现状部分缺) |
| 中 | 输入类型 | 单价/数量用 `type=number`(已有);日期 `type=date`(已有) |
| 中 | 动效 | 过渡统一 150-300ms;`prefers-reduced-motion` 下关闭过渡 |
| 低 | 行操作 | 估算单行已有查看/复制/删除——hover 显现危险项 |
| 通用 | 空/加载/错误态 | 每个列表补统一空态(图标+文案+建议动作);加载骨架屏(首次进入估算单列表可加) |

---

## 七、深色模式(建议第二阶段;含令牌双主题)

- CSS 变量双主题:`html[data-theme=dark] { --bg:#0F172A; --surface:#1E293B; … }`,右上角加"深浅"切换(存 localStorage)。
- 表格网格线/边框在深色下用 `#334155`;正文 ≥4.5:1 独立验证。
- 头部深蓝在深色下可保持或转深炭。图表色板需加深亮度一档。
- 说明:当前图表/网格大量用色底区分公式/必填/错误,深色下需逐格核对,故列为后续。

---

## 八、分阶段实施建议

**P0(纯 CSS + 令牌,低风险,先做)**
1. 引入全部语义化颜色/间距令牌,替换硬编码
2. 头部导航单行化、sysbar 收纳或弱化
3. 卡片/按钮/标签/表格斑马纹+hover+sticky 表头
4. 金额等宽字体列、状态 pill、行 hover 显示危险操作
5. `:focus-visible`、`cursor:pointer`、`prefers-reduced-motion`
6. 图表色板换语义色

**P1(交互反馈,中风险)**
7. 保存按钮 loading 态 + 成功/失败反馈完善
8. 空态图标化、估算单列表顶部统计条
9. 内联 SVG 图标集替换文字按钮(渐进)

**P2(可选,单独评估)**
10. 深色模式(令牌双主题 + 图表/表格逐格核对)
11. 表格虚拟化/`content-visibility`(当数据量大时)
12. 打印样式 `@media print`(预算表上报/归档场景)

---

## 九、反模式(不做)

- ✗ 引入任何外部 CDN/字体/图标库(破坏离线约束)
- ✗ emoji 作导航/按钮图标
- ✗ 紫粉渐变、过度圆角、玻璃拟态(与制造业工具气质不符)
- ✗ 布局大改或组件框架化(无框架项目,保持原生)

---

## 附:设计库来源

- `design_recommend("制造业 成本估算 生产台账 数据表格 图表 内网 Web 工具 专业 清晰")`
- `design_review("制造业成本估算系统:数据表格+图表看板+列表CRUD+表单+登录", platform:web)`
- `design_search("深蓝 工业 数据表格", domain:color)` / `design_search("数据表格 看板 密集 内网工具", domain:style)`

---

## 十、iOS / Apple 风格改造(已实施,commit 见 git log)

用户追加需求:按手机苹果品牌风格进一步优化前端,已落地为 style.css v3。核心手法:

| 维度 | 实现 |
|---|---|
| 背景 | Apple 页面灰 **#F5F5F7**,全浅色基调 |
| 卡片 | 白卡 + 大圆角 18px + 极柔阴影( 2px 8px rgba(0,0,0,.04)),Bento 式分组 |
| 系统色 | 主色 **Apple 蓝 #0071E3**、次级灰 #6E6E73、危险 iOS Red #FF3B30、成功 iOS Green #34C759 |
| 头部 | 深色半透明 + ackdrop-filter: blur(20px) 毛玻璃,标签为 **iOS 胶囊**(active 白底) |
| 登录/弹窗 | 浅色毛玻璃遮罩 + 24px 圆角白卡 + 聚焦光晕(3.5px 蓝 ring) |
| 按钮 | 无边框 + 圆角 + :active scale(.97) 按压反馈 |
| 表格 | 去重边框,改 hairline 细分隔线;表头透明小字;行 hover 淡蓝 |
| 金额/数据 | 等宽字体栈(-apple-system SF 栈优先) |
| 状态 | 可用/草稿标签 iOS pill;toast 深色毛玻璃胶囊 |
| 标题 | 大号 22px/700(类似 iOS 大标题) |
| 分段控件 | 图表模式切换改 iOS segmented(白卡 active 滑块) |

P0 演示截图位于 P0演示-iOS风格/(登录页/估算单列表/预算表编辑器/图表预览/历史对比 5 张)。

---

## 十一、P2 交付增强(已实施,commit 见 git log)

| 功能 | 实现 | 对应原 P2 计划 |
|---|---|---|
| 导出 Excel | 编辑器工具栏「导出 Excel」:预算表 DOM → SpreadsheetML 2003(`.xls`,UTF-8 BOM,`public/js/exporter.js`),优先 `showSaveFilePicker` 自选保存路径,不支持时回退 `<a download>` | —(新增) |
| 导出 PDF | 工具栏「导出 PDF」:调 `window.print()` + `@media print` 打印样式(A4 横向、仅预算表),浏览器"另存为 PDF"自选路径 | P2-12 打印样式(落地) |
| 自动草稿 | 编辑 600ms 防抖写入 `localStorage`(`brick_autosave_v1`);同单/填过名的新单重开提示恢复;保存成功清除;`beforeunload` 未保存提醒 | —(新增) |
| 图表懒加载 | 估算单指纹(名称/日期/状态/行数/成本)缓存,内容未变切页/刷新直接复用,不重复计算(估算单多时不卡) | —(新增) |

- 与既有 iOS 风格一致:导出按钮使用内联 SVG 图标 + 胶囊工具栏,toast 反馈。
- 校验:导出文件用本机 Excel/WPS 打开版式与纸质预算表一致(每行 12 列、标题/分区行按 colspan 展开不错位);打印走浏览器对话框,零第三方依赖。
- 单元测试:`node test/exporter.test.cjs`(结构/类型/转义/数值推断)。
